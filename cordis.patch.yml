# dsh 组合包层（bundle layer）：把梁文谷插入 web 配置树。
#
# 行里的 `name` 必须是包名（而非源码相对路径），Node 解析才能找到已安装的包；
# client-modules 节点半部扫描到这一行，读到 dsh.client 声明后，会把浏览器
# bundle 作为模块挂到 /plugins/liangwengu/client.js。
- insert:
    - id: '梁文谷'
      name: 'liangwengu'
